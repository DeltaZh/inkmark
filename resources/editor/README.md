# Bundled editor styles

MIT-licensed base stylesheet and themes for inkmark.

- `base.css` — editor chrome and content hooks (`#write`, fences, task lists, …)
- `themes/` — bundled themes shipped with the app (default: `github`)

User-imported themes live under the app data `themes` folder. Optional external community theme folders can also be scanned when enabled in settings.
